return {
  -- "neovim/nvim-lspconfig",
  -- opts = {
  --   diagnostics = {
  --     underline = true,
  --     update_in_insert = false,
  --     virtual_text = false, -- Default nvim-lspconfig behavior
  --     severity_sort = false, -- Default is false
  --     signs = true, -- Use default signs
  --   },
  --   inlay_hints = {
  --     enabled = false, -- Default nvim-lspconfig doesn't enable these
  --   },
  -- },
}
